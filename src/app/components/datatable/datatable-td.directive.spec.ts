import { DatatableTdDirective } from './datatable-td.directive';

describe('DatatableTdDirective', () => {
  it('should create an instance', () => {
    const directive = new DatatableTdDirective();
    expect(directive).toBeTruthy();
  });
});
