import { DatatableRowDirective } from './datatable-row.directive';

describe('DatatableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DatatableRowDirective();
    expect(directive).toBeTruthy();
  });
});
